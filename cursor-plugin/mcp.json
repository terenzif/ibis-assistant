{
  "mcpServers": {
    "ibis-assistant": {
      "command": "ibis-assistant",
      "args": ["-mode", "stdio", "-runtime-mode", "plugin"],
      "env": {
        "RUNTIME_MODE": "plugin"
      }
    }
  }
}
